# UI Audit Notes

- Desktop profile rendering still exposes a horizontal-positioning defect: the personal archive content is shifted toward the right edge and clipped in the RTL viewport.
- The public landing and court surfaces render within the expected viewport and retain their hierarchy.
- The remaining repair should force an isolated profile content direction and robust physical centering, then be rechecked at desktop and mobile widths.
